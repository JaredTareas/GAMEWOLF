info:
  name: 9. Recurso no encontrado - videojuego (404)
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/videojuegos/999999"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Un videojuego inexistente devuelve 404", function() {
          expect(res.status).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
