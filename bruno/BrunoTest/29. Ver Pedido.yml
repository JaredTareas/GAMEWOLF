info:
  name: 29. Ver pedido
  type: http
  seq: 29

http:
  method: GET
  url: "{{base_url}}/pedidos/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("El cliente puede consultar el pedido que creo", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.id).to.equal(Number(bru.getVar("pedido_id")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
