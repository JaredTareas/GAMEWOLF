info:
  name: 29. Ver pedido
  type: http
  seq: 29

http:
  method: GET
  url: "{{base_url}}/pedidos/{{pedido_id}}"
  auth:
    type: bearer
    token: "{{token_cliente}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("El cliente puede consultar el pedido que creó", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.id).to.equal(Number(bru.getVar("pedido_id")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
