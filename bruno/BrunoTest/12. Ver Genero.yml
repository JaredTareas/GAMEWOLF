info:
  name: 12. Ver genero
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/generos/2"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Se puede consultar un genero por id", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.id).to.equal(Number(bru.getVar("genero_id")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
