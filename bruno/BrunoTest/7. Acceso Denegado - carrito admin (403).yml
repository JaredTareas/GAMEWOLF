info:
  name: 07. Acceso denegado - carrito de administrador (403)
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/carrito"
  auth:
    type: bearer
    token: "{{token_admin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("El administrador no puede usar un endpoint exclusivo de cliente", function() {
          expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
