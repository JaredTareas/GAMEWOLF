info:
  name: 16. Ver videojuego
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/videojuegos/{{videojuego_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Se puede consultar un videojuego por id", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.id).to.equal(Number(bru.getVar("videojuego_id")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
