info:
  name: 34. Cerrar sesion - administrador
  type: http
  seq: 34

http:
  method: POST
  url: "{{base_url}}/autenticacion/cerrar-sesion"
  auth:
    type: bearer
    token: "{{token_admin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("El administrador puede cerrar sesion", function() {
          expect(res.status).to.equal(200);
          expect(res.body.mensaje).to.be.a('string');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
