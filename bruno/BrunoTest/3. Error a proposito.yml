info:
  name: 3. Error a proposito
  type: http
  seq: 3

http:
  method: POST
  url: https://gamewolf.shop/api/videojuegos
  body:
    type: json
    data: |-
      {
        "titulo": "",
        "precio": "letras"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
