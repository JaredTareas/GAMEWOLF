info:
  name: 08. Sin token - perfil protegido (401)
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/autenticacion/perfil"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Un endpoint protegido rechaza solicitudes sin token", function() {
          expect(res.status).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
