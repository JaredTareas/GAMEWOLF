info:
  name: 22. Ver usuario - admin o empleado
  type: http
  seq: 22

http:
  method: GET
  url: "{{base_url}}/usuarios/{{usuario_id}}"
  auth:
    type: bearer
    token: "{{token_admin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Admin o empleado puede ver un usuario", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.id).to.equal(Number(bru.getVar("usuario_id")));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
